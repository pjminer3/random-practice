const itemList = {};
let initialAmount = null;
let currentBalance = null;
let salesTax = null; 

const initializeSalesTax = (percentage) => {
  // if salesTax is equal to null, reassign it a value of the percentage
}

const initializePriceList = (items) => {
  // items = array of arrays; [productName, SKU, price/unit, taxable?]

  // for each item in items...
    // create a key/value property in itemList
      // itemList[items.SKU] = new createItemObject(individual item);
}

class createItemObject {
  construtor (itemArray) {
    
  }
  // const itemObj = {};
  // // itemObj.product_name: productName
  // // itemObj.price: price/unit
  // // itemObj.taxable: taxable?
  // return itemObj;
}

const initializeStartingAmount = (amount) => {
  // if initialAmount is null (aka it hasn't been initialized), set initialAmount = amount
}

const viewCurrentBalance = () => {
  return currentBalance;
}





